[pytest]
# Pytest configuration for centralized tests

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Minimum Python version
minversion = 3.11

# Output options
addopts =
    -v
    --strict-markers
    --tb=short
    --disable-warnings

# Markers for test categorization
markers =
    unit: Unit tests that don't require external services
    integration: Integration tests with external services
    slow: Slow running tests
    auth: Tests related to authentication
    media_store: Tests related to media store
    inference: Tests related to inference
    broadcaster: Tests related to event broadcasting
